---
phase: 01-base-conectada
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
requirements:
  - CORE-01
  - CORE-02
  - CORE-03
  - CORE-04

must_haves:
  truths:
    - "Sidebar exibe exatamente 5 abas: Saude, Bugs, Features, Melhorias, Alertas — nessa ordem"
    - "Home e Deploys nao aparecem na sidebar nem como views"
    - "Ao fazer login, a view padrao e Saude (nao Home)"
    - "Console mostra [DM Stack] DMS/EDR/RPM: OK apos login"
    - "edrGet e rpmGet retornam dados (ou array vazio) sem erro de CORS"
  artifacts:
    - path: "index.html"
      provides: "Nav reestruturada + constantes EDR/RPM + helpers + verificarConexoes"
      contains: "EDR_URL"
  key_links:
    - from: "iniciarApp()"
      to: "verificarConexoes()"
      via: "chamada direta apos login"
      pattern: "verificarConexoes"
    - from: "edrGet/rpmGet"
      to: "EDR_URL/RPM_URL"
      via: "fetch REST"
      pattern: "EDR_URL.*rest/v1"
---

<objective>
Reestruturar a navegacao do DM Stack para as 5 abas finais (Saude, Bugs, Features, Melhorias, Alertas) e conectar os 3 Supabase (DMS, EDR, RPM) com verificacao no console.

Purpose: Fechar CORE-02 (navegacao correta) e CORE-03 (multi-Supabase) — os dois gaps reais da Phase 1. CORE-01 e CORE-04 ja estao implementados e so precisam ser preservados.
Output: index.html cirurgicamente editado — sem rewrite.
</objective>

<execution_context>
@C:/Users/Duam Rodrigues/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Duam Rodrigues/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/Duam Rodrigues/dmstack/.planning/ROADMAP.md
@C:/Users/Duam Rodrigues/dmstack/.planning/REQUIREMENTS.md
@C:/Users/Duam Rodrigues/dmstack/.planning/phases/01-base-conectada/01-RESEARCH.md
@C:/Users/Duam Rodrigues/dmstack/.planning/phases/01-base-conectada/01-UI-SPEC.md

<interfaces>
<!-- Estado atual do index.html que o executor precisa conhecer -->

Sidebar (linhas 236-274): nav-items com data-view="home|bugs|features|deploys|saude|melhorias"
Views: view-home (L280-307), view-bugs (L310), view-features (L348), view-deploys (L384-406), view-saude (L409), view-melhorias (L463)
JS constants (L641-642): DMS_URL, DMS_KEY
JS helpers (L689-724): dmsGet, dmsPost, dmsPatch, dmsDelete, callEdge
irPara(view) L734-745: switch por view que chama render/carregar
iniciarApp() L1302-1331: carrega tudo em paralelo, inclui carregarDeploys()
Auto-login IIFE L1337-1352: DEVE permanecer como ultimo bloco do script
fazerLogin() L1253: auth via DMS_URL
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reestruturar sidebar e views (CORE-02)</name>
  <files>index.html</files>
  <read_first>
    C:/Users/Duam Rodrigues/dmstack/index.html (linhas 236-274 sidebar, 280-307 view-home, 384-406 view-deploys, 734-745 irPara, 1302-1331 iniciarApp)
  </read_first>
  <action>
Edicoes cirurgicas no index.html — NAO reescrever o arquivo inteiro:

1. **Sidebar (linhas ~236-274):** Substituir o bloco `nav` inteiro dentro de `.sb-nav` por:
   - Remover nav-item `data-view="home"` (Home)
   - Remover nav-item `data-view="deploys"` (Deploys)
   - Remover as duas `sb-section` (Projetos, Sistema)
   - Reordenar para: Saude, Bugs, Features, Melhorias, Alertas
   - Saude fica como primeiro item e recebe `class="nav-item active"`
   - Alertas e um novo item: `<div class="nav-item" data-view="alertas" onclick="irPara('alertas')">Alertas</div>`
   - Manter badges em Bugs e Features (ja existem)

   Resultado final do nav:
   ```
   <nav class="sb-nav">
     <div class="nav-item active" data-view="saude" onclick="irPara('saude')">Saude</div>
     <div class="nav-item" data-view="bugs" onclick="irPara('bugs')"><span>Bugs</span><span class="badge" id="badge-bugs"></span></div>
     <div class="nav-item" data-view="features" onclick="irPara('features')"><span>Demandas</span><span class="badge" id="badge-features"></span></div>
     <div class="nav-item" data-view="melhorias" onclick="irPara('melhorias')">Insights IA</div>
     <div class="nav-item" data-view="alertas" onclick="irPara('alertas')">Alertas</div>
   </nav>
   ```

2. **View Home (linhas ~280-307):** Remover o bloco inteiro `<div id="view-home" class="view active">...</div>`. NAO remover as funcoes JS que ele usava (carregarDadosDash, etc) — elas podem ser uteis em fases futuras.

3. **View Deploys (linhas ~384-406):** Remover o bloco inteiro `<div id="view-deploys" class="view">...</div>`. Manter a funcao `carregarDeploys()` no JS (pode servir no futuro).

4. **Adicionar View Alertas:** Inserir apos view-melhorias:
   ```html
   <!-- VIEW: ALERTAS -->
   <div id="view-alertas" class="view">
     <div class="page-header">
       <div>
         <div class="page-title">Alertas</div>
         <div class="page-sub">Feed unificado de eventos criticos</div>
       </div>
     </div>
     <div style="padding:20px 32px;">
       <div class="lista-empty">Nenhum alerta ainda. Conecte na Phase 5.</div>
     </div>
   </div>
   ```

5. **View Saude:** Adicionar `class="view active"` no view-saude (era view-home que tinha active). Trocar de `<div id="view-saude" class="view">` para `<div id="view-saude" class="view active">`.

6. **irPara() (~linha 734-745):** Remover as linhas de `deploys` do switch. Manter bugs, features, saude, melhorias. NAO adicionar case para alertas (nao tem dados para carregar ainda).

7. **iniciarApp() (~linha 1302-1331):** No `Promise.all`, remover `carregarDeploys()`. Manter `carregarBugs()`, `carregarFeatures()`, `carregarBriefing()`. A chamada `carregarDadosDash()` logo depois pode ficar (usa dados dos bugs/features carregados). No `setInterval` de refresh, remover referencia a deploys se houver.

8. **PRESERVAR INTACTO:** Auto-login IIFE (L1337-1352), fazerLogin(), refreshToken(), todas as funcoes CRUD, todo o CSS, todo o design system. CORE-01 e CORE-04 ja estao ok.

GOTCHAS:
- NUNCA usar appendChild/reparent de elemento existente — so editar innerHTML ou criar novos
- Modal pattern e `.modal-bg.open` — nao alterar
- A IIFE de auto-login DEVE continuar sendo o ultimo bloco do script
  </action>
  <verify>
    <automated>node -e "const h=require('fs').readFileSync('index.html','utf8'); const ok = !h.includes('view-home') && !h.includes('data-view=\"home\"') && !h.includes('data-view=\"deploys\"') && h.includes('view-alertas') && h.includes('data-view=\"alertas\"') && h.includes('data-view=\"saude\"') && h.includes('data-view=\"bugs\"') && h.includes('data-view=\"features\"') && h.includes('data-view=\"melhorias\"'); console.log(ok ? 'PASS' : 'FAIL'); process.exit(ok ? 0 : 1)"</automated>
  </verify>
  <acceptance_criteria>
    - Sidebar tem exatamente 5 nav-items: saude, bugs, features, melhorias, alertas
    - Nenhuma referencia a view-home ou data-view="home" no HTML
    - Nenhuma referencia a view-deploys ou data-view="deploys" no HTML (view removida, funcao JS pode ficar)
    - view-saude tem class="view active" (view padrao apos login)
    - view-alertas existe como placeholder
    - irPara() nao referencia 'deploys'
    - iniciarApp() nao chama carregarDeploys() no Promise.all
    - IIFE de auto-login continua como ultimo bloco do script
  </acceptance_criteria>
  <done>Sidebar reestruturada com 5 abas finais, views Home/Deploys removidas, Alertas placeholder adicionado, Saude como view padrao</done>
</task>

<task type="auto">
  <name>Task 2: Conectar 3 Supabase com verificacao no console (CORE-03)</name>
  <files>index.html</files>
  <read_first>
    C:/Users/Duam Rodrigues/edr-system/index.html (buscar por supabase.co para encontrar anon key EDR)
    C:/Users/Duam Rodrigues/rpmpro/v2/infra.js (buscar por supabase.co para encontrar anon key RPM)
    C:/Users/Duam Rodrigues/dmstack/index.html (linhas 637-724 para ver constantes e helpers existentes, linhas 1302-1331 iniciarApp)
  </read_first>
  <action>
Edicoes cirurgicas no index.html — adicionar constantes e funcoes, NAO mexer no que ja funciona:

1. **Constantes EDR e RPM (logo apos DMS_KEY, ~linha 643):** Adicionar:
   ```javascript
   const EDR_URL = 'https://mepzoxoahpwcvvlymlfh.supabase.co';
   const EDR_KEY = '...'; // copiar a anon key EXATA de ~/edr-system/index.html
   const RPM_URL = 'https://roeeyypssutzfzzkypsq.supabase.co';
   const RPM_KEY = '...'; // copiar a anon key EXATA de ~/rpmpro/v2/infra.js
   ```
   IMPORTANTE: Ler os arquivos fonte e copiar as keys literais. NAO inventar keys.

2. **Helpers edrGet e rpmGet (apos callEdge, ~linha 724):** Adicionar:
   ```javascript
   function edrGet(t, q = '') {
     return fetch(`${EDR_URL}/rest/v1/${t}${q}`, {
       headers: { 'apikey': EDR_KEY, 'Authorization': 'Bearer ' + EDR_KEY }
     }).then(r => r.json()).catch(() => null);
   }

   function rpmGet(t, q = '') {
     return fetch(`${RPM_URL}/rest/v1/${t}${q}`, {
       headers: { 'apikey': RPM_KEY, 'Authorization': 'Bearer ' + RPM_KEY }
     }).then(r => r.json()).catch(() => null);
   }
   ```
   ATENCAO: Usar EDR_KEY/RPM_KEY como Bearer (acesso anonimo), NUNCA o _token do DMS — armadilha 2 do RESEARCH.md.

3. **Funcao verificarConexoes (apos os helpers acima):** Adicionar:
   ```javascript
   async function verificarConexoes() {
     const [dms, edr, rpm] = await Promise.allSettled([
       fetch(`${DMS_URL}/rest/v1/bugs?limit=1`, {
         headers: { 'apikey': DMS_KEY, 'Authorization': 'Bearer ' + (_token || DMS_KEY) }
       }),
       fetch(`${EDR_URL}/rest/v1/companies?limit=1`, {
         headers: { 'apikey': EDR_KEY, 'Authorization': 'Bearer ' + EDR_KEY }
       }),
       fetch(`${RPM_URL}/rest/v1/oficinas?limit=1`, {
         headers: { 'apikey': RPM_KEY, 'Authorization': 'Bearer ' + RPM_KEY }
       })
     ]);
     console.log('[DM Stack] DMS:', dms.status === 'fulfilled' && dms.value.ok ? 'OK' : 'FALHA');
     console.log('[DM Stack] EDR:', edr.status === 'fulfilled' && edr.value.ok ? 'OK' : 'FALHA');
     console.log('[DM Stack] RPM:', rpm.status === 'fulfilled' && rpm.value.ok ? 'OK' : 'FALHA');
   }
   ```

4. **Chamar verificarConexoes em iniciarApp():** Adicionar `verificarConexoes();` apos o `Promise.all` e `carregarDadosDash()` em `iniciarApp()`. Nao precisa de `await` — roda em background.

GOTCHAS:
- RLS pode retornar [] com status 200 — verificar response.ok (HTTP status), NAO array.length
- CORS: Supabase libera para qualquer origin com apikey — sem config extra
- As anon keys sao publicas por design — nao sao secrets
  </action>
  <verify>
    <automated>node -e "const h=require('fs').readFileSync('index.html','utf8'); const ok = h.includes('EDR_URL') && h.includes('EDR_KEY') && h.includes('RPM_URL') && h.includes('RPM_KEY') && h.includes('edrGet') && h.includes('rpmGet') && h.includes('verificarConexoes') && h.includes('mepzoxoahpwcvvlymlfh') && h.includes('roeeyypssutzfzzkypsq'); console.log(ok ? 'PASS' : 'FAIL'); process.exit(ok ? 0 : 1)"</automated>
  </verify>
  <acceptance_criteria>
    - EDR_URL e EDR_KEY presentes com URL mepzoxoahpwcvvlymlfh.supabase.co
    - RPM_URL e RPM_KEY presentes com URL roeeyypssutzfzzkypsq.supabase.co
    - Keys sao as anon keys REAIS copiadas dos projetos (nao placeholders)
    - edrGet() e rpmGet() usam EDR_KEY/RPM_KEY como Bearer (nao _token)
    - verificarConexoes() usa Promise.allSettled e verifica r.value.ok
    - verificarConexoes() e chamada dentro de iniciarApp()
    - DMS_URL/DMS_KEY/dmsGet/dmsPost/dmsPatch/dmsDelete permanecem intactos
  </acceptance_criteria>
  <done>3 Supabase conectados (DMS, EDR, RPM) com helpers de leitura e verificacao no console apos login</done>
</task>

</tasks>

<verification>
1. `node -c index.html` — nenhum erro de sintaxe (se o JS inline for valido)
2. Abrir index.html localmente (npx serve -s .), fazer login, verificar:
   - Sidebar mostra: Saude, Bugs, Features, Melhorias, Alertas
   - View padrao apos login e Saude
   - F12 Console mostra [DM Stack] DMS/EDR/RPM: OK (ou FALHA se RLS bloqueia, mas sem erro de rede)
   - Clicar em cada aba funciona sem erro
   - Clicar em Alertas mostra placeholder
</verification>

<success_criteria>
- Sidebar com 5 abas corretas na ordem: Saude, Bugs, Features, Melhorias, Alertas
- Sem Home e sem Deploys na navegacao
- Console mostra status dos 3 Supabase apos login
- Nenhuma funcionalidade existente quebrada (login, CRUD bugs/features, modais, detail drawer)
</success_criteria>

<output>
After completion, create `.planning/phases/01-base-conectada/01-01-SUMMARY.md`
</output>
